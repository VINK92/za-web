import { createBasicTheme } from './create-basic-theme';

const lightTheme = createBasicTheme({
  palette: {
    primary: {
      main: '#137F7F', // primaryDefault
    },
  },
});

export { lightTheme };
